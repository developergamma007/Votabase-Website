Place the production Votabase Android APK here as:

```text
votabase.apk
```

The login page downloads `/downloads/votabase.apk` locally and `/ui/downloads/votabase.apk` when deployed with `basePath: "/ui"`.
